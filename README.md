## DtFromTxtExtractor

A C# tool to extract fixed-width text tables, infer column schemas,
and generate strongly typed C# classes automatically.

### Features
- Fixed-width text parsing
- Automatic column width detection
- Type inference with confidence threshold (95% rule)
- Nullable detection
- C# class generation

### Example

Example_WeatherStations.txt
